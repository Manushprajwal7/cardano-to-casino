# Operator-specific configurations
operator:
  id: "operator-b"
  name: "Atlantic City Gaming"
  did: "did:prism:..."
  signature_required: true
  settlement_auto_approve: true
  max_concurrent_sessions: 100

  game_limits:
    max_bet_per_round: 50000
    max_payout_per_session: 500000
    allowed_game_types: ["slots", "blackjack", "poker", "roulette"]
